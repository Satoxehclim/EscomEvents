import 'package:flutter/material.dart';

class AppColors {
  // --- TEMA CLARO (Opción 2: Vida Estudiantil) ---
  static const Color lightPrimary = Color(0xFF4F46E5);   // Indigo Eléctrico
  static const Color lightSecondary = Color(0xFF0EA5E9); // Turquesa
  static const Color lightAccent = Color(0xFFF43F5E);    // Coral (para alertas/likes)
  static const Color lightBackground = Color(0xFFFFFFFF);
  static const Color lightSurface = Color(0xFFF3F4F6);   // Gris muy suave para tarjetas

  // --- TEMA OSCURO (Opción 3: Tech Dark) ---
  static const Color darkPrimary = Color(0xFF2979FF);    // Neon Blue
  static const Color darkSecondary = Color(0xFF00E676);  // Mint Green (éxito/acción)
  static const Color darkBackground = Color(0xFF121212); // Negro Material Standard
  static const Color darkSurface = Color(0xFF1E1E1E);    // Gris oscuro para tarjetas
}